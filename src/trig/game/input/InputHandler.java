package trig.game.input;

import trig.listener.GameListener;

/**
 * The InputHandler's job is to handle what inputs are needed.
 * in effect, it controls the listeners, as, you do not always need
 * to listen for events you are not going to handle.
 * 
 * InputHandler will handle the construction and de-construction of listeners.
 * 
 * @author brody
 *
 */

public class InputHandler 
{
	private GameListener gameListener;
	//private MouseInput mouseInput;
	//private KeyBoardInput keyBoardInput;
	
}
